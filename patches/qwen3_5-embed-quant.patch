Route the token embedding table through vLLM's quantized-embedding path
(CompressedTensorsEmbeddingWNA16Int). The kernel exists upstream; the qwen3_5
model code just never passes quant_config to VocabParallelEmbedding. Two
hunks: the main model, and the MTP draft module (without the second one,
single-user mode crashes on load with "no parameter named
'embed_tokens.weight_packed' in Qwen3_5MultiTokenPredictor").

Apply from the installed vllm package directory:
  patch -p1 -d venv/lib/python3.12/site-packages/vllm < patches/qwen3_5-embed-quant.patch

Validated against vLLM 0.28.0. Reapply after upgrades.

--- exported from cpuchip/vllm dabae6094 (qwen3_5-embed-quant); regenerate with scripts/export-patch.sh, do not edit ---

diff --git a/model_executor/models/qwen3_5.py b/model_executor/models/qwen3_5.py
index f991f05d7..fe960984b 100644
--- a/model_executor/models/qwen3_5.py
+++ b/model_executor/models/qwen3_5.py
@@ -245,6 +245,8 @@
         self.embed_tokens = VocabParallelEmbedding(
             self.vocab_size,
             config.hidden_size,
+            quant_config=self.quant_config,
+            prefix=maybe_prefix(prefix, "embed_tokens"),
         )
 
         def get_layer(prefix: str):
diff --git a/model_executor/models/qwen3_5_mtp.py b/model_executor/models/qwen3_5_mtp.py
index 4c064bead..384142f56 100644
--- a/model_executor/models/qwen3_5_mtp.py
+++ b/model_executor/models/qwen3_5_mtp.py
@@ -84,6 +84,8 @@
         self.embed_tokens = VocabParallelEmbedding(
             self.vocab_size,
             config.hidden_size,
+            quant_config=vllm_config.quant_config,
+            prefix=maybe_prefix(prefix, "embed_tokens"),
         )
 
         # Workaround: mtp.fc is stored as BF16 in NVFP4 checkpoints but is
